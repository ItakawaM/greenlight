global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "greenlight-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:${METRICS_PORT}"]

  - job_name: "greenlight-exporter"
    metrics_path: /metrics
    static_configs:
        - targets: ["exporter:${METRICS_PORT}"]